pipeline {  
    agent any 
    stages { 
        stage('Lint Dockerfile'){ 
            steps{
                echo "Pipeline Usando Jenkinsfiless"
            }
        }
        stage('Build image'){ 
            steps{
                echo "Pipeline Usando Build image"
            }
        }

    } 
}
