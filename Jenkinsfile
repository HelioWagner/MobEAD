pipeline {  
    agent any 
    stages { 
        stage('Lint Dockerfile'){ 
            steps{
                echo "Pipeline Usando Jenkinsfile"
            }
        }
    } 
    stages { 
        stage('Build image'){ 
            steps{
                echo "Pipeline Usando Build image"
            }
        }
    } 
}
